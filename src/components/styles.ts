import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  button: {
    backgroundColor: "#819986", // cor desejada
    padding: 16,
    borderRadius: 8,
    alignItems: "center",
    marginTop: 16,
  },
  title: {
    color: "#fff",
    fontSize: 18,
    fontWeight: "bold",
  },
});
